import 'package:flutter/material.dart';

class HealthDataType extends StatelessWidget {
  const HealthDataType({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}