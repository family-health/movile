import 'package:flutter/material.dart';

enum icon { google, apple }

class IconButton extends StatelessWidget {
  const IconButton({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}